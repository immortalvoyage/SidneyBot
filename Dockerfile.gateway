FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev
COPY gateway.js ./
ENV NODE_ENV=production
ENV GATEWAY_HEALTH_PORT=8788
EXPOSE 8788
CMD ["node", "gateway.js"]
